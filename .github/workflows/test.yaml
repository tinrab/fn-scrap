name: Test

on:
  push:
    branches:
      - develop
      - "feature/*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: 11.x
      - name: Test
        run: ./gradlew test
